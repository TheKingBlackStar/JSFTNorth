rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read featured event images
    match /featuredEvents/{imageId} {
      allow read: if true;
      // Allow write/delete for any authenticated user (admin panel handles authorization)
      allow write, delete: if request.auth != null;
    }
  }
}
